server:
  port: 8083
spring:
  application:
    name: RATING-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/micro-rating
    username: root
    password: Shubham3417@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  instance:
    prefer-ip-address: true
    #allow to use IP address for eureka server discovery
  client:
    fetch-registry: true
    #allow this service to fetch eureka server
    register-with-eureka: true
    #will make the service a eureka client
    service-url:
      defaultZone: http://localhost:8761/eureka
    #defining the eureka server location